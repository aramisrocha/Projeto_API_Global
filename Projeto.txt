
Api Global


- Subir a rede, testar - OK 
- definir o que vai fazer a api OK
- Testar API - OK 
- Subir a rede na segunda região OK
- Criar as duas funçoes  em cada regiao lambda com a api
- Criar uma Banco de dados no RDS Aurora expandido para a as duas regoes
- Alterar a API para gravar no Banco 
- Colocar o APlication load Balance na frente das Lambdas 
- Verificar se é necessario criar um VPC link 
- Subir a API no api gateway
- Critar hosted zone no route 53 
- Adicionar os registros como filover e testar




Exemplo 1 — API de Cotações (Aurora Global Database)

API Gateway (Regional) em duas regiões (ex: us-east-1 e sa-east-1).

Cada API aciona uma Lambda que busca dados da bolsa (ex: via API externa).

Os dados são gravados no Aurora Global Database (writer em uma região, replica global na outra).

Route 53 com Latency-Based + Failover aponta os usuários para a API mais próxima e redireciona se uma região cair.